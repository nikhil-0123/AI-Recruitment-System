events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name localhost;

        location / {
            return 200 'AI Recruitment System Infrastructure is Live!';
            add_header Content-Type text/plain;
        }
    }
}